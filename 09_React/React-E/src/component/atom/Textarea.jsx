import React from "react"
const Textarea = ({className,id}) => {
    return (
        <>
        <textarea className={className} id={id} rows='5'></textarea>
        </>
    )
}
export default Textarea